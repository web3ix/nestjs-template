export enum Role {
  USER = 'user',
  MERCHANT = 'merchant',
  SUPPORTER = 'supporter',
  OPERATOR = 'operator',
  ADMIN = 'admin',
}

export enum KycLevel {
  LEVEL_1 = 'level_1',
  LEVEL_2 = 'level_2',
  LEVEL_3 = 'level_3',
}

export enum AuthProvider {
  LOCAL = 'local',
  GOOGLE = 'google',
  TELEGRAM = 'telegram',
}
